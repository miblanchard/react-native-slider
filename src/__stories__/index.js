/* @flow */
import React from 'react';
import {storiesOf} from '@storybook/react-native';
import SliderExample from '../../SliderExample/src/App';

storiesOf('Video', module).add('Scrubber', () => <SliderExample />);
